#include <stdio.h>
main()
{
	int a, b;
	 printf("entre value of a ");
	 scanf("%d",&a);
	 
	 printf("entre value of b ");
	 scanf("%d",&b);
	 
	 if (a>b)
	 {
	 	printf("a max");
	 }
	 else if (b>a)
	 {
	 	printf("b max");
	 }
	 else
	 {
	 	printf("both are same");
	 }
}
	 
